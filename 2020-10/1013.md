# 10월 13일 TIL

# Linux , Environment Variable

***환경 변수는 부모 프로세스에서 자식프로세스로 전송됩니다.***

Environment Variable : name(대문자) = value , 명령어 env

### Environment List

- global 변수 "environ" 을 사용할 수 있게 설정해야 한다.

    `#include <stdlib.h>`
    `extern char **environ;`

- string 여러개 들어있는데 끝에 '\0'이 들어있다.
- 맨 마지막 string 뒤에는 null pointer가 들어있다.

![Untitled](https://user-images.githubusercontent.com/55980680/96016234-787e9b80-0e83-11eb-96b6-5196d872e62a.png)

environ에 list를 가르치고 있는 포인터가 들어있다.

첫번째를 가르키게 되면 string이 쭉 연결된다. 마지막에 NULL pointer가 자리 잡고 있다.


---

### getenv() : 특정 변수의 값을 알아내기 위한 함수

```c
#include <stdlib.h>

char *getenv(const char *name); // name은 변수 이름, string으로 반환
```

### putenv() : 변수에 write할 때 사용하는 함수

```c
#include <stdlib.h>

int putenv(const char* str)
/*변수 이름을 대묹로 적고 값은 str로 적는다. 변수에 대응되는 값이 바뀐다
만약에 없을 경우 add가 된다 */
```

### setenv() / unsetenv() : 환경변수를 등록시키는 함수

```c
#include <stdlib.h>

int setenv(const char *name, const char*value, int rewrite);
/* name: 변수 이름 value : 변수 내용 , rewrite : 의사표현*/

int unsetenv(const char* name) //제거하는 함수
```

---

## 알고리즘

- [합병정렬](https://blog.naver.com/rhdwn6580/222114856718)
- [퀵소트](https://blog.naver.com/rhdwn6580/222114900448)
- [동적계획법](https://blog.naver.com/rhdwn6580/222114940320)
- [floyd warchall](https://blog.naver.com/rhdwn6580/222115369472)

## 한줄평

환경변수 정말 열심히 공부했는데 ,,,,,,시간이 부족해서 코드를 못짰다,,,,,,,,,,,,, C언어는 너무 어려운 거 같다,,,,,, 알고리즘도 열심히 공부했는데 학교시험은 나를 무기력하게 만든다,,, 슬프다😭
