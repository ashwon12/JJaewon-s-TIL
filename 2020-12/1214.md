# 12월 14일의 TIL

## python에서 xml 파싱하는 방법

나는 xml파일을 json으로 변환해서 requests 모듈을 통해 데이터를 가져올 생각이었는데 

ElementTree 라고 xml에서 파싱하는 모듈이 존재했다!

### ElementTree 모듈

python 2.5에서부터 내부 라이브러리로 포함되었고 xml을 다룰 때 많이 사용한다.

xml은 계층적 데이터 구조이고 표현하기 위해서 tree를 사용한다. 

`ElementTree` : xml 전체 문서 ,전체문서와의 상호작용, 읽기,쓰기 할 때 ㅅㅏ용

`Element` : 트리에서의 단일 노드 , 단일 xml 요소 및 하위 요소와의 상호작용



### 예제
```python
    import requests
    import xml.etree.ElementTree as ET
    
     # 문자열로 된 xml을 파싱 , tree에 xml 파일 전체를 가져온다.
    tree = ET.fromstring(res.text) 
    
    # 저장된 xml 파싱
    tree = ET.parse('data.xml')
    root = tree.getroot()

    # 특정 태그(body/items/item)의 전체를 가져오기
    child = tree.findall('./body/items/item')
    
    # item 하위의 모든 태그 가져오기
    chlid = item.findall('./'):
    
```


## 한줄평

올 해 초부터 자격증 모아보기,, 이런거에 꽂혀서 api와 씨름을 하고 있는데

머리가 터질거 같다!! 자격증 왜이렇게 많아!!!! 

전공이랑 자격증 연동만 하면 ,,, 이제 끝이 보이는데 어렵다,,,

배열이랑 딕셔너리 너무 헷갈린다 ㅎㅎ



