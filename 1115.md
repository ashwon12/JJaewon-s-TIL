# 11월 15일의 TIL

## android

### fragment간 데이터 전달

Fragment main에서 Fragment 1,2,3로 데이터를 전달할 때 재생성을 위해 newInstance()를 사용한다.

- **Fragment_main**
```java
    public static Fragment_main newInstance() {
        Fragment_main fragment = new Fragment_main();
        return fragment;
    }
```
데이터를 전달받지 않기 때문에 Fragment-main의 instance에는 파라메터가 없다.

```java    
    public View onCreateView(@NonNull LayoutInflater inflater, @Nullable ViewGroup container, @Nullable Bundle savedInstanceState) {
        v = inflater.inflate(R.layout.fragment_main, container, false);
        
        Button.OnClickListener buttonListener = new Button.OnClickListener() {
            @Override
            public void onClick(View view) {
                et_main=(EditText)v.findViewById(R.id.et_main);
                String input = String.valueOf(et_main.getText());
                switch (view.getId()){
                    case R.id.btn_a:
                        ((MainActivity)getActivity()).setFragment(FragmentA.newInstance(input));
                        break;
                    case R.id.btn_b:
                        ((MainActivity)getActivity()).setFragment(FragmentB.newInstance(input));
                        break;
                    case R.id.btn_c:
                        ((MainActivity)getActivity()).setFragment(FragmentC.newInstance(input));
                        break;
                }

            }
        };
    }
```
입력받은 값을 해당 프래그먼트의 instance의 파라메터로 전달시켜준다. instance를 통해 코드의 중복을 피할 수 있다.

<br>

- **FragmentA**
```java
   public static FragmentA newInstance(String input) {
        FragmentA fragmentA = new FragmentA();
        Bundle args =  new Bundle();
        args.putString("text",input);
        fragmentA.setArguments(args);
        return fragmentA;
    }
 ```
 fragment_main에서 전달받은 값을 번들로 전달시킨다. 값이 여러개일때 번들을 사용하면 좋다.
 
 ```
         if(getArguments() != null){
            String newText = getArguments().getString("text");
            et_a.setText(newText);
        }
 ```
onCreateView() 메소드에서 값을 받아 사용하면 된다.

<br>

- **MainActivity**
```java
    public void setFragment(Fragment fragment){
        FragmentManager fragmentManager = getSupportFragmentManager();
        FragmentTransaction fragmentTransaction = fragmentManager.beginTransaction();
        fragmentTransaction.replace(R.id.frame, fragment).commit();
    }
```
프래그먼트간 교체가 일어날 때 사용하는 메소드를 선언해주어야 한다.


<br><br>

## 한줄평
newInstance가 이해가 안되서 몇시간을 삽질했는지 모르겠다... 

머리가 안좋아서 자꾸 밤을 새는거 같다...  그래도 이해했으니까 됐다,,😥

프래그먼트 데이터를 이렇게 전달하는 방법도 있었구나!

    
