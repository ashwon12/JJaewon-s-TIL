# 10월 8일 공부한 내용

## Android

🔻***오늘의 커밋내용🔻***

### 라디오버튼 사용법

1. `radiogroup` 안에 radiobutton 그룹화 ( 라디오버튼 안에 들어있는 버튼들은 한 명만 클릭 가능!) 
2. java클래스에서 radiogroup만 값 선언해준다.

    ```java
    radioGroup=(RadioGroup)findViewById(R.id.radioGroup);
    ```

3. 선택된 값 가져오기

    ```java
    int id = radioGroup.getCheckedRadioButtonId();
    RadioButton selectedButton = (RadioButton) findViewById(id);
    Toast.makeText(LoginActivity.this
    							,selectedButton.getText().toString(),Toast.LENGTH_SHORT).show();
    ```

### DIalog & Toast 커스텀

1. layout.xml 파일 생성해서 UI 디자인하기
2. JAVA 클래스에서 view로 지정해서 inflate 해주기

    ```java
    dialogview=(View)View.inflate(MainActivity.this,R.layout.dialog,null);
    AlertDialog.Builder dlg = new AlertDialog.Builder(MainActivity.this);

    toastview=(View)View.inflate(MainActivity.this,R.layout.toast,null);
    Toast toast = new Toast(MainActivity.this);
    ```

3. 속성값들 달아주기
    - dialog 속성들
    `dlg.setTitle();`  타이틀 설정
    `dlg.setIcon();`   아이콘 설정

        `dlg.setMessage();`  내용입력

        `dlg.setPositiveButton(msg, Listner);`  true 값 버튼 눌렀을 때

        `dlg.setNagativeButton(msg,Listner);`  false값 버튼 눌렀을 때

4. 보여주기

    ```java
    dlg.setView(dialogview);
    dlg.show();
    ```

## 한줄평

 안드로이드 스튜디오랑 공기계 핸드폰 USB 연결이 안되서 한시간 이상을 뻘짓했다,,, 근데 한시간 해도 연결이 안돼서 케이블선 바꿔서 다시 해보ㅏㅇㅑ 한다 ㅠㅠㅠ 아까운 시간,,, 리눅스 공부할 시간을 날렸다 😭
