# 11월 23일의 TIL

## python 으로 네이버 검색 API 사용해서 검색 결과 가져오기

```python

    headers = {
        'X-Naver-Client-Id': 'API',
        'X-Naver-Client-Secret': '비밀번호',
    }
    params = {
        'query': detail_certis + ' 후기',
        'display': '5',
        'start': '1',
        'sort': 'sim',
    }
    response1 = requests.get('https://openapi.naver.com/v1/search/blog.json', headers=headers, params=params)
    response2 = requests.get('https://openapi.naver.com/v1/search/cafearticle.json',headers=headers, params=params)
    blog_review = response1.json()
    cafe_review = response2.json()
    return jsonify({'result': 'success', 'blogData' : blog_review,'cafeData':cafe_review})
    
 ```
 
naver 검색 API를 신청한 다음 Client ID와 Client-Secret 키를 받아서 헤더로 입력해준다음

요청에 필요한 파라메터들을 전달해주면 된다. api를 가져오는 부분은 get 방식으로 진행되고 네이버 블로그와

카페 둘 다 정보를 가져올 수 있다.

<br><br>

## 한줄평

이렇게 정리하고 보니까 별 내용 없는데 나는 또 엄청 오래 걸렸다.

검색하고자 하는 값을 사용자한테 받아와야하는데 받아오는 값이 html 파일에 있다.

그래서 html에서 파이썬 라우터로 값을 전달하지 않고 ajax로 바로 url에 값을 요청해서 받아오면 훨씬 간편할 거라고 생각했다.

그렇게 구현하려고 하는데 요청값을 아무리 보내도 받지를 못한다... 방법이 있는데 내가 못찾은 것 같다.

그래서 우선 이렇게 api를 통해 통신하는 방법으로 구현했다!!!! 알아내면 그 방법까지 포함해서 블로그에 글써야지

그리구 주말 내내 notion 블로그 정리하고 넥스터즈 지원서 쓰느라 공부는 많이 못했다 ㅠㅠ 엉엉 시험기간 빠이팅


